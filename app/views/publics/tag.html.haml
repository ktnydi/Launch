- @page_title = params[:category]
%main.article_list
  .article_list_box
    .container
      %h2.heading_2
        %span.sharp #
        = params[:category]
      = render "share/card", publics: @publics
