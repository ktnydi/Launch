.post-comments
  - if comments.present?
    %h3 この記事のコメント
    - comments.each do |comment|
      .user-comment-wrapper
        = image_tag gravatar_image(comment.user), size: 60
        .comment
          .md-comment
            != markdown comment.content
          .comment-bottom
            .comment-user
              = comment.user.name
              ・
              \#{comment.created_at.strftime("%Y.%m.%d")}
            - if user_signed_in? && comment.user_id === current_user.id
              .comment-delete
                = link_to "削除", "/comment/#{comment.id}/", method: :delete, remote: true, data: { confirm: "コメントを削除します。"}
