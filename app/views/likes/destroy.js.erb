var hearts = document.getElementsByClassName('heart_wrapper')
for (var i = 0; i < hearts.length; i++) {
  const heart = hearts.item(i)
  heart.innerHTML = '<%= j(render "posts/post", public: @public) %>'
}
